{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "build": {
      "commands": ["bash config/build.sh"]
    }
  },
  "deploy": {
    "inputs": [{ "step": "build" }],
    "startCommand": "bash config/start.sh"
  }
}
